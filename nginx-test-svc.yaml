apiVersion: v1
kind: Service
metadata:
  name: nginx-test-svc1
spec:
  selector:
    app: nginx-test
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
